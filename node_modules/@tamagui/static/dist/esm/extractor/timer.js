const timer = () => {
  const start = Date.now();
  let last = start;
  return {
    mark: (name, print = !1) => {
      if (print) {
        const took = Date.now() - last;
        last = Date.now(), console.log(`Time ${name}: ${took}ms`), took > 10 && console.log("  long timer");
      }
    },
    done: (print = !1) => {
      if (print) {
        const total = Date.now() - start;
        console.log(`Total time: ${total}ms`), total > 50 && console.log("  long timer");
      }
    }
  };
};
export {
  timer
};
//# sourceMappingURL=timer.js.map
