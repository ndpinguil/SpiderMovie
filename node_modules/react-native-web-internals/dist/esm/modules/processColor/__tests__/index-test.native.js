import processColor from "..";
const platformSpecific = (x) => x;
describe("processColor", () => {
  describe("predefined color names", () => {
    it("should convert red", () => {
      const colorFromString = processColor("red"), expectedInt = 4294901760;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    }), it("should convert white", () => {
      const colorFromString = processColor("white"), expectedInt = 4294967295;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    }), it("should convert black", () => {
      const colorFromString = processColor("black"), expectedInt = 4278190080;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    }), it("should convert transparent", () => {
      const colorFromString = processColor("transparent"), expectedInt = 0;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    });
  }), describe("RGB strings", () => {
    it("should convert rgb(x, y, z)", () => {
      const colorFromString = processColor("rgb(10, 20, 30)"), expectedInt = 4278850590;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    });
  }), describe("RGBA strings", () => {
    it("should convert rgba(x, y, z, a)", () => {
      const colorFromString = processColor("rgba(10, 20, 30, 0.4)"), expectedInt = 1711936542;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    });
  }), describe("HSL strings", () => {
    it("should convert hsl(x, y%, z%)", () => {
      const colorFromString = processColor("hsl(318, 69%, 55%)"), expectedInt = 4292558252;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    });
  }), describe("HSLA strings", () => {
    it("should convert hsla(x, y%, z%, a)", () => {
      const colorFromString = processColor("hsla(318, 69%, 55%, 0.25)"), expectedInt = 1088109996;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    });
  }), describe("hex strings", () => {
    it("should convert #xxxxxx", () => {
      const colorFromString = processColor("#1e83c9"), expectedInt = 4280189897;
      expect(colorFromString).toEqual(platformSpecific(expectedInt));
    });
  });
});
//# sourceMappingURL=index-test.js.map
